# Sniffle ATT/GATT Fuzzer 使用指南

> 配套文档：《进度.md》（当前状态与下一步）、《设计.md》（为什么这么设计、平台事实、阶段路线图）、《固件开发避坑指南.md》（编译/烧录）。
> 本文讲"怎么用"：从零跑通一次 fuzz 会话、读结果、分析崩溃、扩展语料。

---

## 0. 前置条件

| 项 | 要求 |
|---|---|
| 硬件 | CC2652R1F LaunchPad（XDS110 板载），udev 规则已装（无需 sudo） |
| 固件 | **1.12.0 补丁固件**（0x28 门控 + TX 队列满上报 + TERMINATE reason 上报）。`probe_fw_version()` 应返回 `1.12.0`；缺补丁时时序用例与掉链原因判定不可用 |
| Python | python3 + `pyserial` + `pyyaml`（`pip install pyserial pyyaml`） |
| 目标 | 一个"GATT 免配对即可访问"的 BLE 外设（阶段一：耳机/鼠标）。需知 MAC 或广播名片段 |
| 环境 | 板子必须插着；目标设备在广播状态（耳机开盖/配对模式最稳） |

---

## 1. 三步快速开始

### 第 1 步：填目标档案

编辑 `att-fuzz/targets/headphone.json`，二选一：

```json
{
  "name": "headphone",
  "mac": "A4:C1:38:xx:xx:xx",        // 方式一：直接给 MAC
  "search_string": "",                 // 方式二：广播名片段（如 "EDIFIER"）
  "mac_random": true,                  // MAC 是随机地址(蓝牙耳机基本是)
  "conn_interval": 12,                 // 连接间隔 12×1.25ms = 15ms(越小 fuzz 越快)
  "latency": 0,                        // 外设延迟 0 = 每个事件必响应,判定最稳
  "connect_timeout": 10
}
```

`mac` 和 `search_string` 至少一个非空。用广播名的好处：耳机每次重连地址可能变（RPA 轮换），按名找不会断。`mac_random` 用 `true/false` 或 `1/0` 均可（1=随机地址，0=public）；不确定时先跑 `--probe` 看实测的地址类型再定。

### 第 2 步：冒烟（发现 GATT，不跑语料）

```bash
python3 att-fuzz/runner.py --target att-fuzz/targets/headphone.json --discover-only
```

预期输出：`ll_max=251 att_mtu=247`、服务列表、`GATT 地图已保存`。这一步同时验证：串口/固件/连接/DLE/MTU/发现全链路。**任何一步卡住，先看 §6 排查。**

顺带验证 TERMINATE 上报（补丁 #2）：上面跑通后，用 `--replay` 注入一条无害 PDU 并在另个终端看 `transport.jsonl`，或直接跑一次小语料后检查台账里掉链用例带不带 reason 码。

### 第 3 步：首跑（50 用例冒烟）

```bash
python3 att-fuzz/runner.py --target att-fuzz/targets/headphone.json --max-cases 50
```

跑完看 `logs/run-<时间戳>/` 下三个产物（§4）。分类分布正常应大量 `ERROR_RESPONSE` + 少量 `OK_RESPONSE`，几乎无 `TIMEOUT`。确认无异常后再放开跑全量（不加 `--max-cases`）或过夜。

---

## 2. CLI 全量参考

```
python3 att-fuzz/runner.py \
  --target att-fuzz/targets/headphone.json \
  [--strategy att-fuzz/strategies]        # 策略目录/单个 yaml,默认全目录
  [--seed 1]                              # random 值内容的确定性种子
  [--max-cases 0]                         # 0=全量(默认)
  [--outdir logs/run-xxx]                 # 输出目录(默认 logs/run-<时间戳>)
  [--serport /dev/ttyACM0]                # 串口(默认自动探测 XDS110)
  [--discover-only]                       # 只连接+发现,不跑语料
  [--replay <PDU_HEX>]                    # 重放:注入指定 ATT PDU 后即止
  [--replay-case <CASE_ID>]               # 从台账按 case_id 找 PDU 重放
  [--ledger <ledger.jsonl>]               # replay-case 用的台账(默认最新 run)
  [-v]                                    # DEBUG 日志
```

常用组合：

```bash
# 全量语料(约几百用例,一次连下一个多小时)
python3 att-fuzz/runner.py --target ... 

# 只跑某层
python3 att-fuzz/runner.py --target ... --strategy att-fuzz/strategies/offsets.yaml

# 崩溃复现:先 --replay 原始 PDU(连一次打一条)
python3 att-fuzz/runner.py --target ... --replay 0a0000

# 按台账复现(自动找那条用例的 PDU)
python3 att-fuzz/runner.py --target ... --replay-case h-read-0000 --ledger logs/run-xxx/ledger.jsonl

# 阶段三:反向角色 -- 伪装 GATT server 打手机(攻击面⑧)
# 板子广播可连接 peripheral,手机系统蓝牙/nRF Connect 扫描连接后,对手机
# ATT 请求回正常模式响应(假 GATT 服务表);手机断开自动重新广播。
python3 att-fuzz/runner.py --target att-fuzz/targets/xisem鼠标.json --server \
  --server-name "Sniffle Server" --server-duration 300 --adb-serial ZD9L8H454HDY7DEU
#  --server-duration 0 = 一直跑到 Ctrl-C(默认)
#  --adb-serial:logcat oracle 目标手机序列号;adb 缺失/手机未连则 oracle fail-open
# 产物: logs/run-<ts>/capture.pcap + transport.jsonl + server_ledger.jsonl(手机请求->我方响应对)
#       + crashes/(oracle 命中落盘的崩溃窗口)
# oracle 离线自检/回放:
#   cd att-fuzz && python3 -m core.adb_oracle --selfcheck
#   python3 -m core.adb_oracle --replay-file /path/logcat.txt
```

---

## 3. 攻击面与语料（当前阶段）

阶段一实现攻击面 ①②③④（⑤⑥⑦⑧ 与 0x28 时序用例标了阶段二，见《设计.md》§八，但 `transport.inject(att_pdu, gate_at=N)` 已预留门控能力）。

| 层 | 文件 | 内容 |
|---|---|---|
| ① opcode | `strategies/opcodes.yaml` | 保留 opcode、合法\|0x40/0x80 翻转、0xE0-0xFF 原始注入 |
| ② handle | `strategies/handles.yaml` | 0x0000/0xFFFF/0xFFFE/0x8000、真实 handle ±1、声明/CCCD handle、发现类 start>end/全0/全1 |
| ③ value | `strategies/values.yaml` | 长度 0/1/20/MTU-3/MTU-2/MTU-1/MTU/MTU+5/244/247/255 × 内容模式 |
| ④ offset | `strategies/offsets.yaml` | 0/1/baseline_len±1、0x7FFF/0x8000/0xFFFF，read_blob 与 prepare_write 双打 |

**语料锚点语法**（写自己的策略时用）：

```yaml
- id: my-case-1
  layer: value
  op: write_req
  handle: "${each.value}"        # 按每个特征展开(结合 filter)
  value: {len: "${mtu-3}", pattern: incremental}
  filter: writable               # 只对含 write 属性的特征展开;readable 同理
```

- `${each.value}` 值 handle、`${each.decl}` 声明 handle、`${each.cccd}` CCCD、`${each.baseline_len}` 该特征基线值长度
- `${mtu}` 协商 MTU、`${mtu-3}`、`${baseline_len+1}` 算术
- `value.pattern`: `zero / ff / incremental / random / fmtstring / ascii`
- op 全集：`read_req / read_blob_req / write_req / write_cmd / prepare_write_req / execute_write_req / find_info_req / read_by_type_req / read_by_group_type_req / read_multiple_req / exchange_mtu_req`
- ①层原始注入：`{opcode: 0x00, payload: "0100"}`（不解析参数直接发）

**确定性**：同 `--seed` 同 GATT 地图 → 每次语料完全一致（random 内容由 seed+case_id 派生）。复现靠它。

---

## 4. 输出产物（logs/run-<时间戳>/）

| 文件 | 内容 | 用途 |
|---|---|---|
| `ledger.jsonl` | **每用例一行**：case_id/layer/分类/opcode/handle/offset/value_len/value_hash/event/error_code/terminate_reason/前后健康状态/`replay.pdu` | 判定与复现的源头 |
| `capture.pcap` | 全量 LL 无线包（含用例边界 marker 包） | Wireshark 打开（DLT 256），按 marker 定位到具体用例的帧 |
| `transport.jsonl` | 传输层原始事件（注入/接收/掉链/DLE/MTU/固件 debug） | 排查传输层问题 |
| `gatt_map.json` | 本次发现的服务/特征/CCCD/基线 | 下次可直接复用（GATT 变了会自动重发现） |

**分类含义**：

| 分类 | 含义 | 动作 |
|---|---|---|
| `OK_RESPONSE` | 正常（非错误）响应 / 无响应类用例完成 | 无 |
| `ERROR_RESPONSE` | ATT Error Response，带 error_code（0x01 Invalid Handle 等） | 无 |
| `TIMEOUT` | 超时无响应（2×连接间隔+2s） | 关注：可能目标栈忙/异常 |
| `DISCONNECT_TERM` | 目标发 TERMINATE，带 reason 码。**reason=0x08 (LL Protocol Error) 是"栈崩了"的强信号** | 立即复现 |
| `DISCONNECT_SUP` | 目标静默（supervision timeout），无 TERMINATE | 复现 |
| `TX_QUEUE_FULL` | 传输层错误（队列满/未连接），用例无效 | 重跑该用例 |
| `HEALTH_DEGRADED` | 用例"完成"但后置健康检查异常（迟滞显现） | 重点看 |
| `ATT_FREEZE` | ATT 层冻结：post-HC 健康检查读无响应，但 LL 链路存活（无 TERMINATE、无 supervision 断链）；重连即恢复 | 立即复现——最高价值信号之一 |

**崩溃分析流程**：

```bash
# 1. 找出异常。运行日志在终端 stderr(建议跑时重定向一份:
#    python3 att-fuzz/runner.py --target ... 2>logs/run-xxx/run.log &
#    然后 grep ALERT run.log),或直接看台账分类分布:
python3 -c "
import json
c={}
for l in open('logs/run-xxx/ledger.jsonl'):
    r=json.loads(l); c[r['classification']]=c.get(r['classification'],0)+1
print(c)"

# 2. 复现单条(自动从台账取 PDU)
python3 att-fuzz/runner.py --target ... --replay-case <case_id> --ledger logs/run-xxx/ledger.jsonl

# 3. 复现成功 → 保留 pcap,用 marker 时间定位帧,提取最小化
# 4. 复现失败/偶发 → 同一用例多连几次(--replay 可循环跑),关注前后健康检查字段
```

---

## 5. 维护与扩展

### 离线测试（不需要板子，改动后必跑）

```bash
python3 att-fuzz/tests/test_offline.py   # 编解码/L2CAP 重组/语料展开/确定性
python3 att-fuzz/tests/test_dryrun.py    # FakeHw 模拟 GATT server,走通全流程
```

### 加一层语料

在 `strategies/` 加 yaml（见 §3 语法），或给现有层补用例后直接跑。展开规模=模板数×命中特征数，注意别把 `${each.*}` 配到不需要的 `filter` 上把语料撑爆。

### 换目标设备

复制 `targets/headphone.json` 成 `targets/<设备>.json`，改 MAC/广播名/连接参数。不同设备 GATT 结构不同，语料会自动按发现结果展开（锚点来自运行时 GATT 地图，不写死）。

### 阶段二钩子（现在不用动）

- `transport.inject(att_pdu, gate_at=N)`：0x28 门控已实现，`cur_event` 事件号已跟踪，只等语料层加时序用例
- 掉链恢复里的"广播重现=重启检测"已留 `_recover()` 重试骨架，深度检测是下一步
- `server_fuzz.py`（反向角色打手机）与 `relay_fuzz.py` 目录已留空

---

## 6. 常见问题排查

| 现象 | 原因与处理 |
|---|---|
| `Sniffle device not found` | 板子没插/udev 规则没生效。`ls /dev/ttyACM*`，`--serport` 手动指；不行就重插 |
| 连接永远失败 + 日志里**完全看不到目标广播** | **MAC 字节序**（2026-08-29 踩坑数小时）：固件全线用线序（小端）。transport 的 `_parse_mac` 已自动反转，但如果你自己写脚本直接调 `cmd_mac`/`initiate_conn`，必须传线序（`64:44:7B:EE:41:F4` -> `F4 41 EE 7B 44 64`），否则 RF core 永远匹配不上且无任何报错 |
| 目标广播正常但连接失败（0x1408 STOPPED / RXERR） | 嘈杂环境下 initiator 命令被外来广播干扰；固件补丁 3/4（INITIATING 重试 + 8 深 RX 队列）已处理。确认固件是 1.12.0（`--probe` 或版本探测），且 connect 序列里 `cmd_auxadv(True)`（False 会让 RF core 死追 aux 指针） |
| vivo TWS 等耳机盒内闲置后连不上 | 设备节电怪癖：广播照发但 LL 不 ACK 新 CONNECT_IND。开盖激活即可恢复（对照实验：开盖后基线 5/5 成功） |
| `connection timed out` 后板子无响应 | host 在 INITIATING 中途退出会让固件卡死在 forever initiator 命令里，transport 已自动 cmd_reset 自愈；自写脚本需注意 |
| `target not found by advertisement string` | 广播名片段不匹配。用手机 nRF Connect 看真实广播名，或直接填 MAC |
| `no LL_LENGTH_RSP`（日志 warning） | 目标不支持 DLE，MTU 退化为 23，大值用例自动变成"超长非法 PDU"打法，不影响运行 |
| 台账大量 `TIMEOUT` | 目标反应慢/栈弱，可把 target 档案 `conn_interval` 调大（如 24）降速重试 |
| `TX queue full`（transport.jsonl 的 fw_debug） | 发端限速没拦住，通常是目标不消费（半死）。会重连，可接受 |
| `HEALTH_DEGRADED` 高频 | 健康检查读的值本身就老变（动态特征如电量）。`known_good_handle` 优先 Device Name，若目标是动态值多的设备，可手动在 gatt_map.json 里把 `baseline` 清掉重发现 |
| 复现不了某条 DISCONNECT | 掉链可能是连接参数太激进或时序抖动。`--replay` 多打几次，或把 `--seed` 换掉看是否影响 |
| GATT 地图与手机 nRF Connect 对不上 | 先看 `gatt_map.json` 的 `gaps`（发现期异常记录）；很多耳机有动态服务，重发现一次 |

---

## 7. 当前状态与下一步

见《进度.md》——状态类信息只在那一处维护，本文不再重复。

---

## 8. GUI 可视化控制台（NiceGUI）

> 跨平台（Windows/macOS/Linux），浏览器渲染（可 `--native` 原生窗口），与 CLI 共享同一套 core。**串口按任务懒占用**：GUI 空闲（未运行任何任务）时不碰串口，CLI 可正常使用；只有点了探测/发现/Fuzz/Replay 才打开串口，任务结束自动释放。任务运行期间由锁文件互斥（`/tmp/sniffle-hw-*.lock`，记录持有者 PID，持锁进程死亡后自动失效），冲突时立即报错并指明占用方，无需靠串口报错猜原因。

### 启动

```bash
pip install nicegui                       # 依赖
python3 att-fuzz/gui/app.py               # 浏览器模式 http://localhost:8765
python3 att-fuzz/gui/app.py --native      # 原生窗口(需 pip install pywebview)
python3 att-fuzz/gui/app.py --port 9000 --host 0.0.0.0   # 允许局域网远程访问串口机
python3 att-fuzz/gui_demo.py              # 离线演示:FakeHw 模拟,无硬件
```

### 四个页面

| 页面 | 功能 |
|---|---|
| 控制台 | 串口选择/固件版本检查（<1.12.0 告警）、目标档案新建/编辑/删除（targets/*.json，删除带确认）、策略勾选 + seed/max-cases、探测广播/仅发现/开始 Fuzz、暂停/继续/停止、GATT 地图树、运行日志 |
| 实时仪表盘 | 进度/速率/用时统计卡、分类分布环形图、累计用例曲线、连接健康（链路/事件号/MTU/重连/TX 队列）、告警列表、传输层事件流（可按 kind 过滤） |
| 结果与重放 | 台账表格（按分类/攻击层/关键词过滤），点行看详情（响应 PDU、replay.pdu），一键 Replay ×1/×5，导出 Markdown 摘要 |
| 历史会话 | logs/run-* 列表，勾选两个会话做分类分布对比图，单会话摘要导出 |

### 典型流程

1. 控制台选目标档案（或编辑保存）→ **探测广播**确认目标可见、地址类型匹配
2. **仅发现 GATT** 看地图树 → **开始 Fuzz**（先用 max-cases=50 冒烟）
3. 仪表盘盯告警；发现告警后到结果页点行 → **Replay ×5** 验证可复现
4. 会话产物与 CLI 完全一致（`logs/run-*/ledger.jsonl` + pcap），CLI 的 `--replay-case` 照常可用

### 注意

- **GUI 空闲 ≠ 占串口**：仪表盘"连接健康"里的"串口"chip 显示"空闲 / 占用(GUI xx任务, /dev/ttyACMx)"，可直接判断 CLI 现在能不能用。锁同样约束 CLI 侧（fuzz/发现/探测/扫描工具都会先拿锁）。
- 暂停/停止的生效粒度是**单个用例边界**（run_case 最多阻塞一个响应超时 ≈2s；若目标掉链在自动重连中，停止要等恢复流程结束）。
- Replay 会重新连接目标并占用串口；运行中不可再启动其他任务。
- 演示模式（`gui_demo.py`）里探测广播无意义（FakeHw 不模拟广播），目标选 `__demo__`。
