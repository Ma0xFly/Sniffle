# Sniffle ATT/GATT Fuzzer 设计

> 本文回答"为什么这么设计、系统怎么工作、分几步走"。当前做到哪一步见《进度.md》,怎么操作见《使用指南.md》,固件编译烧录见《固件开发避坑指南.md》。
> 一切设计以"平台事实"一节为依据——它们是逐行核对固件/Python 源码得出的,改设计前先核对本节。

---

## 一、输入模型

一切 fuzz 输入 = **(opcode, 参数元组, 时序)** 的笛卡尔积:

```
opcode 空间(22 个合法值 + 保留值 + 标志位翻转)
  × 参数空间(handle / offset / value 长度 / value 内容)
  × 时序空间(在哪个连接事件发、连发几条、发完等不等)
```

这个全积是十亿级,永远不遍历。语料策略是两层剪枝:①确定性边界值优先(命中已知 bug 类型的概率 > 随机,且可复现可最小化);②模板 × 锚点稀疏展开,配合按属性过滤(见"语料设计")。真正的组合爆炸源是时序维度(阶段二),那一层靠变异抽样而非遍历。

## 二、平台事实(逐行核对,设计依据)

1. **`cmd_transmit(llid, pdu, event)` 的 `event` 参数不控制发送时机**——它只是固件跟踪 LL 控制过程 instant 用的记账字段(`RadioTask.c:1327 reactToTransmitted`),PDU 实际在下一个连接事件按 FIFO 出队(`RadioTask.c:476`)。时序维度由固件补丁 `COMMAND_TRANSMIT_AT (0x28)` 提供。
2. **TX 队列标称深度 8、实际可用 7**(满判定差=7,`TXQueue.c:50`),队列满时旧固件静默丢包(`CommandTask.c:120` 无视返回值)——补丁后 dprintf 上报。fuzzer 发端限速,未决 PDU 恒 ≤4。
3. **事件计数器 host 可读**:连接态每个 RX 数据包都被贴上 `connEventCount`(`fw/PacketTask.c:230-233`),host 侧 `PacketMessage.event`(`packet_decoder.py:39,77`)直接可读。host 能从最近收到的包确定当前事件号,`cmd_transmit_at` 可确定性调度"第 K 事件发 N 条"。注意 `TXQueue_take_at` 只取队头起最长合法前缀(`TXQueue.c:103-133`)——**0x28 门控事件号必须单调递增入队**,否则后面的包会卡住前面的。
4. 直连目标需完整连接前置:`cmd_chan_aa_phy` -> `cmd_pause_done` -> `cmd_follow(False)` -> MAC 过滤 -> `cmd_setaddr` -> `mark_and_flush` -> `initiate_conn(interval=6~24, latency=0)` -> 等 `StateMessage(CENTRAL)` -> 设 `decoder_state.cur_aa`(参照 `initiator.py` / `relay_master.py:380-397`)。
5. **加密边界**:Sniffle 无 SMP。直连只在"GATT 无需加密/配对即可访问"时成立(多数电车 BLE 裸奔,可行);需配对的会话走 relay 双腿(已配对会话中继)。
6. **单帧 ATT PDU 上限 247**:LL data payload 物理上限 251 字节,ATT PDU = 251 − 4 字节 L2CAP 头 = 247,即有效 ATT MTU ≤ 247。Exchange MTU 里的 517 等只能作"声明值"。>247 的 ATT PDU 必须自做 L2CAP 分片:首条 LLID=2(带 4 字节 L2CAP 头),续条 LLID=1;收方向固件不做重组,host 必须按 LLID 自己拼。
7. **大 MTU 前置 DLE**:固件不协商 LL Length Extension。目标在收到 `LL_LENGTH_REQ`(控制 PDU,llid=3,opcode 0x14)之前只接受默认 27 字节 LL 帧(= ATT MTU 23)。fuzzer 作为 central 必须先发 `LL_LENGTH_REQ(251,251)`,再发 ATT Exchange MTU Request,才能测大 value。
8. **LL_TERMINATE_IND reason 码需补丁 #2 上报**:固件收到 TERMINATE 只触发 `handleConnFinished()`(`RadioTask.c:1068-1071`),reason 不上报 host。补丁 #2 在 case 0x02 里加 `reportMeasTerminate(reason)`(measurement 通道)。
9. Python 侧必须 select 事件循环(照 `relay_master.py:238-244` 写法),绝不阻塞等待。固件 RX 队列仅 8 深(`PacketTask.c:64`),密集响应期要控制节奏防丢包。
10. **MAC 字节序**(2026-08-29 踩坑数小时):固件全线(`cmd_mac` 过滤器 + `initiate_conn` 的 pWhiteList)用**线序**(小端,`F4 41 EE 7B 44 64`),人类书写序(`64:44:7B:EE:41:F4`)必须反转后再发(`transport._parse_mac` 已处理)。反序的后果:连接永远失败 + 日志里完全看不到目标广播(被 MAC 过滤器吞掉),且无任何报错。
11. 固件全链路无 L2CAP 层:分片/重组/MTU 跟踪全在 host 侧(`transport._L2capReassembly`)。

## 三、攻击面八层

| # | 攻击层 | 注入位置 | 目标 Bug 类型 | 优先级 |
|---|---|---|---|---|
| ① | Opcode 空间 | 未知 opcode(0x00/0x1F/0x40+/0xE0-0xFF);合法 opcode\|0x40/0x80 | 解析器未处理分支、断言、越界表索引 | P1 |
| ② | Handle 空间 | 0x0000/0xFFFF/0xFFFE、真实 handle ±1、服务组间隙、0x8000;对只读/声明 handle 发写 | 无效 handle 错误路径、权限绕过 | P0 |
| ③ | Value 内容 | 长度 0/1/20/MTU-3/MTU-1/MTU/MTU+1/244/247+L2CAP 分片;0xFF/0x00/递增/随机/格式串/NUL | 堆/栈溢出、截断、分配器 | P0 |
| ④ | Offset 空间 | 0/len-1/len/len+1/0x7FFF/0x8000/0xFFFF,配合 (offset+len) 越过缓冲边界 | OOB 读写(Quarkslab 主战场) | P0 |
| ⑤ | 状态机违规 | 未协商 MTU 就发读写;同事件多发 2+;Execute Write 空队列;二次 MTU Exchange | 死锁、状态机混乱(SweynTooth 手法) | P1(阶段二,依赖 0x28+事件号同步) |
| ⑥ | 资源耗尽 | Prepare 队列填满不 Execute;短连接快速连断;超长值连续长写 | 内存泄漏、堆耗尽、UAF | P0(阶段二) |
| ⑦ | CCCD 语义 | 0x2902 写 0x0000/1/2/3/0300/FFFF;对无 Notify 属性订阅;订阅抖动 | 逻辑错误、事件分发混乱 | P1(阶段二) |
| ⑧ | 反向角色 | Sniffle 伪装恶意 GATT server 打手机 client | 客户端栈崩溃(BlueZ/GD) | P2(阶段三) |

### 核心攻击点详解

**② Handle——先枚举再打洞**。连接后用发现引擎拿到真实 handle 集,变异集 = 真实 handle ∪ 边界值(0x0000/0x0001/0xFFFF/0xFFFE/0x8000)∪ 真实 handle ±1±2 ∪ **服务组间隙**(两个相邻真实 handle 之间的整段,取中点+两端——很多实现这里解析逻辑最薄弱)。每个 handle 跑 opcode 子集:Read(0x0A)/Read Blob(0x0C)/Write(0x12)/Write Cmd(0x52)/Prepare(0x16)。**特别关注对只读 handle 发写、对声明 handle(0x2800/0x2803 所在处)发写**——权限校验路径的代码往往和正常路径是两套。

**④ Offset——OOB 富矿**。offset 集 = {0, 1, len-1, len, len+1, ATT_MTU-1, ATT_MTU, 0xFFFF, 0x8000},关键配对原则:让 `(offset + value_len)` 恰好 == 缓冲大小 / == 缓冲+1 / 远大于缓冲,在边界两侧各打一发。Quarkslab 打爆 Espressif example 的堆越界写就是 `offset + value_len > PREPARE_BUF_MAX_SIZE(512)` 时直接 memcpy。offset 是 16 位全空间,配合 value 长度做边界探测性价比最高,不需要随机。

**③/⑥ Prepare/Execute 双阶段缓冲——最容易挂的路径**(几乎所有栈这里都有队列管理代码):
```
- 单条 Prepare(offset=0)→ 不 Execute → 断连          # 队列泄漏
- 连续 100 条 Prepare(合法值)→ 不 Execute → 观察内存   # 队列填满/溢出
- 100 条 Prepare → 异序 offset(0, 512, 0, 256...)→ Execute
- Execute 0x00(取消)→ 继续 Read 该属性                  # 状态残留
- Execute 0x01(空队列提交)                             # 空指针/UAF
- Execute 0x02/0xFF(非法 flags)                        # 未处理分支
```

**⑤ 同事件多发**。SweynTooth 打 STM32 的思路:一个 connection event 里塞 2 条 ATT 请求(CVE-2019-16336 死锁)。用 0x28 门控把两条 PDU 排到同一事件,观察死锁/挂起。

**⑦ CCCD 抖动**。值集 {0x0000, 0x0001, 0x0002, 0x0003, 0x0100, 0x0200, 0x0300, 0xFFFF, 0x5A5A};对每个已发现的 0x2902 连写三个不同值(中间不等 Notification 稳定);**对无 Notify 属性的特征值 handle 直接写 0x0100**——很多栈不校验"这个特征能不能通知"。

**⑧ 反向角色(打手机 client)**。Sniffle 用 `cmd_advertise()` 广播恶意 GATT server,手机连上后:服务发现时回**零长度 Find Info 响应**(CVE-2024-3077 的触发方式);回超长 attribute value(> 声明长度);对 Read 回 ATT_MTU-1 畸形数据;Notification 洪水。crash 判定:logcat 里 bluetooth 进程崩/重启、蓝牙服务无响应。

**语料补强**(零成本):handle 区间 fuzz(发现类 opcode 的 start>end / 0,0 / 全 1 区间)、Read Multiple 句柄表边界(空表/奇数/重复/超 MTU)、MTU 协商值集(23/24/251/517/非法)、L2CAP SDU 长度撒谎(声明长度 > 实际 fragment,阶段二)、同事件多发(依赖补丁 0x28,阶段二)。

## 四、语料设计

**确定性优先**:`random` 内容由 `seed + case_id` 派生——同 seed 同 GATT 地图 → 语料逐字节一致,复现靠它。第一轮只跑确定性边界值集;变异引擎(阶段二)才在确定性语料产出的"新签名"种子上做随机化。

**展开机制**(`core/corpus.py expand()`):
- YAML 模板里的 `${each.value}/${each.decl}/${each.cccd}/${each.baseline_len}` 锚点按 GATT 地图逐特征展开;`${mtu}/${mtu-3}/${baseline_len+1}` 等标量表达式按会话上下文解析
- `filter: writable/readable` 按 props 位图剪枝(对不可写 handle 不打写用例)
- `${each.*}` 展开规模 = 模板数 × 命中特征数,**别把 filter 配到不需要的模板上把语料撑爆**
- 展开后按 PDU 字节去重(不同模板在边界处常撞出相同字节)
- `value.pattern`: `zero / ff / incremental / random / fmtstring / ascii`
- ①层原始注入:`{opcode: 0x00, payload: "0100"}`(不解析参数直接发)

**变异引擎(阶段二设计)**:黑盒拿不到 coverage,AFL 式反馈换成**响应签名当伪覆盖**——`signature = 分类 | 响应opcode | error_code | term_reason | hc异常`(台账已有此列)。种子 = 产生新签名的用例 + 告警用例;变异 = bit/byte 翻转 + handle/offset/len 字段的边界邻近变异(**不做纯随机字节**——ATT PDU 结构性强,乱翻 opcode 字节就退化成未知 opcode 轰炸);能量调度 = 新签名加权、永远同错误码的降权;时序维度用 `gate_at` 抽样。

## 五、架构

```
att-fuzz/
├── core/
│   ├── transport.py      # SniffleTransport:select 事件循环、inject(llid,pdu,gate_at)
│   │                     #   队列深度限速、L2CAP 分片/重组、pcap+JSONL 双录、
│   │                     #   事件号跟踪、DLE/MTU 协商、连接自愈
│   ├── att.py            # ATT 编解码:全 opcode、req/rsp 配对、MTU 状态
│   ├── gatt_map.py       # 发现引擎(服务→特征→CCCD→基线) -> GattMap 持久化
│   ├── corpus.py         # 模板×锚点展开、去重(见"语料设计")
│   ├── session.py        # 连接生命周期:健康检查、自动重连、断链恢复续跑
│   ├── monitor.py        # 分类/台账/响应签名
│   └── serial_lock.py    # 串口锁文件(GUI/CLI 跨进程互斥)
├── roles/
│   ├── central_fuzz.py   # 模式一:直连外设(请求-响应节奏推进)——已实现
│   ├── server_fuzz.py    # 模式二:假 GATT server 打手机(阶段三)
│   └── relay_fuzz.py     # 模式三:relay 双腿注入(阶段四)
├── strategies/           # 攻击面表 = YAML 语料(${anchors} 锚点)
├── targets/*.json        # 目标档案:MAC/搜索串、连接参数、配对约束
├── gui/                  # NiceGUI 控制台(与 CLI 共用 core)
└── runner.py             # CLI:probe/discover/fuzz/replay/replay-case
```

**主循环(所有模式共用,判定纪律是核心)**:

```
每用例: 健康检查(known-good read) -> 注入 -> 等响应(超时=2×连接间隔+2s)
-> 分类{正常/错误响应/超时/TERMINATE(reason)/掉链/静默} -> 双录落盘 -> 健康检查
-> 挂了: 记 crash, 等广播重现(重启检测), 重连重发现, 从下一用例续跑
```

前后双健康检查区分"第 N 条打挂"和"第 N-3 条就挂了现在才显现"——黑盒归因全靠它。健康锚点 = 设备名 0x2A00(优先)或任何基线可读的静态 handle。

**台账最小复现单元**:`(case_id, mode, seed, opcode, handle, offset, value_len, value_hash, event, pcap偏移, 队列状态)` + `replay <case_id>` 复现命令;响应签名列支持变异引擎的语料进化。

## 六、固件补丁规格(1.11.0 → 1.12.0)

1. **`COMMAND_TRANSMIT_AT (0x28)` 门控发送**:PDU 在 TX 队列中等到连接事件计数达到指定值才发出,实现确定性"第 K 事件发 N 条"(同事件多发、排队后释放)。老命令 0x19 与 relay 完全兼容(gateAt=0 不门控)。涉及 `CommandTask.{c,h}`、`TXQueue.{c,h}`(新增 16 位 gateAt trailer + `TXQueue_take_at`)、`RadioTask.c`(CENTRAL/PERIPHERAL 出队点换 `TXQueue_take_at`)。Python 侧 `SniffleHW.cmd_transmit_at()`。
2. **TX 队列满上报**:`TXQueue_insert` 返回 false 时 `dprintf("TX queue full, PDU dropped")`,fuzzer 归为传输层错误而非靶子信号。
3. **TERMINATE reason 上报**:`measurements.{c,h}` 加 `MEASTYPE_TERMINATE=6` + `reportMeasTerminate(reason)`;`RadioTask.c` case 0x02 在 `handleConnFinished()` 前调用;`python_cli/sniffle/measurements.py` 加 `TerminateMeasurement` 解析。
4. **INITIATING 态失败重试**:密集 RF 环境下 initiator 命令经常早死(STOPPED/RXERR);原逻辑回 PAUSED 让 host 重新发起,补丁改为 INITIATING 态原地重试(`Task_sleep(10)` 节流)。
5. **RX 队列 2→8 深**(`RadioWrapper.c` `NUM_DATA_ENTRIES`):密集环境广播流量把 2 深队列撑爆(BLE_ERROR_RXOVF 0x1805)杀死 initiator。
6. ~~maxWaitTimeForAuxCh=0~~(已回退):实测 0 是病态值导致全连不上(0x1403 RXERR),保持原生 0xFFFF;连接序列须 `cmd_auxadv(True)`,False 会让 RF core 死追 aux 指针。
7. 版本号 1.11.0 → 1.12.0(`measurements.c`),fuzzer 用 `probe_fw_version()` 探测能力,<1.12.0 时序用例与掉链 reason 判定不可用。

## 七、Crash 判定(oracle 分三档)

| 信号 | 判定 | 动作 |
|---|---|---|
| 协议内:ATT Error Response 内容 / 响应超时 / LL TERMINATE_IND reason 码[补丁#2] / 广播消失重现(=重启) | 硬崩/挂起/连接级异常 | 存最小复现单元,重连续跑 |
| 目标侧可观测:Android logcat(btservice 崩/重启)+ tombstone | 客户端崩溃 | 同上(阶段三) |
| 边信道(可选):电车 CAN 总线嗅探等 | 行为异常 | 记录 |

注:连接态单射频无法同时扫描,"广播重现"检测需断链->重扫->重连,只在异常掉链怀疑崩溃时做深度检查;掉链本身(supervision timeout 约 500ms -> StateMessage)是主信号。**比"是否断链"更有信息量的是断链方式**:有 TERMINATE reason 是优雅退出;静默死(supervision timeout,无 TERMINATE)= 栈冻结,强信号。

## 八、阶段路线图

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| **一**:central 直连 + 确定性语料 | transport/att/gatt_map/session/monitor/central_fuzz/runner + 攻击面①②③④,实测目标=免配对蓝牙耳机;MTU 策略:连接后自动 LL_LENGTH_REQ(251,251) → Exchange MTU(声明 517),按目标实际响应定协商值 | 免配对耳机全链路跑通;全量语料可复现(同 seed 同结果);告警用例可 --replay-case 复现;自动重连过夜跑;手头有 ESP32 则做 Quarkslab Prepare Write OOB 复现自检 |
| **二**:状态机/资源/CCCD + 时序 + 变异引擎 | 攻击面⑤⑥⑦ + 0x28 时序用例(同事件多发、跨事件排布)+ L2CAP SDU 撒谎 + 变异引擎(签名驱动抽样与能量调度) | 时序用例稳定注入(同事件多发可复现);变异轮次中新签名语料持续增长;⑤⑥⑦ 用例产出并与阶段一告警对照 |
| **三**:反向角色打手机 | server_fuzz 应答核(Android-first,iOS oracle 不可用),攻击面⑧ | 手机 client 栈 crash 可触发且 logcat 可归因 |
| **四**:relay 双腿注入 | relay_fuzz:已配对会话中继 + 双向篡改(突破"免配对"限制) | 已配对设备(手机↔外设)会话中可注入且不破坏链路 |
| **五**:电车目标 | 先识别其 BLE 模组厂商(Telink/WCH/ESP32 系,决定能否复刻同构栈做祭品),再套用前面全部能力 | 电车 GATT 免配对面跑通全量语料 |
