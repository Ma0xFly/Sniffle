# Sniffle ATT/GATT Fuzzer 进度

> **当前所处:阶段一收尾**(告警用例复现中),阶段二未启动。阶段定义与验收标准见《设计.md》§八。
> 新会话接手:先读本文件 → 需要理解设计再读《设计.md》→ 要跑起来看《使用指南.md》。
> 更新约定:每完成一个功能/发现重要事实,更新对应小节并在文末追加一行。

## 当前状态(2026-08-30)

**已完成**:
- 阶段一四层语料(opcode/handle/value/offset),模板展开 → PDU 去重后 572 条
- 固件 1.12.0 补丁烧录生效(0x28 门控/TX 满上报/TERMINATE reason/INITIATING 重试/RX 8 深);**fw 改动未提交**,分支 att-fuzz-fw,产物 `sniffle_cc2652r1_fuzz_1.12.0.hex`
- CLI 全套:probe / discover-only / fuzz / replay / replay-case
- GUI(NiceGUI):目标档案新建/编辑/删除(带确认)、四个页面、串口状态 chip
- 串口锁(跨进程互斥,GUI 空闲不占串口,CLI 可用)
- 运行日志持久化(logs/gui.log,跨重启回放)+ 每页面独立游标
- 历史页识别任意目录名(以产物文件为准)、含 GATT 发现列
- gatt_map.json 人工友好排版;台账新增 signature 列(黑盒伪覆盖,为变异引擎打底)

**实测结果(vivo TWS 3e,`64:44:7B:EE:41:F4` public,两次全量 663 条可复现)**:
- 分布:407 ERROR / 239 OK / 9 HEALTH_DEGRADED / 8 TIMEOUT
- 高价值信号:8 条未知 opcode(0x1f/0x15/0x53/0x4a/0x42/0xd3/0xe0/0xe1)全部 TIMEOUT;
  全程约 8 次**静默掉链且无 LL TERMINATE**(栈冻结而非优雅断链);
  Prepare Write offset=0xFFFF、512 字节大值写触发 HEALTH_DEGRADED
- 目标怪癖:耳机盒内闲置后广播仍在但拒绝新连接(LL 不 ACK),开盖恢复;长跑保持开盖

**技术债 / 已知缺口**:
- GUI `_make_transport` pcap=None:GUI 跑 fuzz 不产 capture.pcap(CLI 有)——深挖告警前修
- `logs/dryrun-test`(自测产物)混进历史页,未决定排除
- `state.take_logs` 已被 `logs_since` 取代,未删
- docs 未进 git(untracked),含本文件;git 身份配置后统一提交

## 下一步(按优先级)

1. 复现告警用例:`--replay-case o-prepare-ffff@001d`、`v-len-mtu-incr@0018`、任一 `op-*`
   (台账在 logs/run-20260830-181858/)——确定是否为可报告漏洞
2. GUI fuzz 补 pcap
3. 阶段二:变异引擎(签名驱动,设计见《设计.md》§四)+ ⑤⑥⑦ 语料 + 0x28 时序用例
4. 阶段三及以后:见《设计.md》§八路线图

## 开发纪律

- 改 core 后必跑:`python3 att-fuzz/tests/test_offline.py` + `test_dryrun.py`
- 板子:CC2652R1F LaunchPad,串口 /dev/ttyACM0(数据口);GUI/CLI 靠 serial_lock 互斥
- MAC 用线序(小端)!`64:44:7B:EE:41:F4` → `f441ee7b4464`(transport 已处理,自写脚本注意)
- 目标免配对才可直连;需配对等 relay 模式(阶段四)
- 健康检查锚点=设备名 0x2A00;动态值特征会误报 HEALTH_DEGRADED

## 更新记录

- 2026-08-30(文档重构):思路.md 并入 fuzz-plan.md 改为《设计.md》并删除;
  状态类内容全部收拢到本文件;使用指南 §7 改为指向本文件
- 2026-08-30:PDU 去重(663→572)/台账 signature 列;串口锁/GUI 持久化/历史页改造/
  gatt_map 排版;vivo 两次全量 663 条,17 告警,静默掉链发现
- 2026-08-29 前:阶段一完成 + 固件补丁(细节见《设计.md》§六与《使用指南.md》)
