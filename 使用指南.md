# Sniffle ATT/GATT Fuzzer 使用指南

> 配套文档：《进度.md》（当前状态与下一步）、《设计.md》（为什么这么设计、平台事实、阶段路线图）、《固件开发避坑指南.md》（编译/烧录）。
> 本文讲"怎么用"：从零跑通一次 fuzz 会话、读结果、分析崩溃、扩展语料，覆盖全部模式（直连/加密冒充/反向角色/被动嗅探/离线解密）与原版 Sniffle 工具。

---

## §0 前置条件

| 项 | 要求 |
|---|---|
| 硬件 | CC2652R1F LaunchPad（XDS110 板载），udev 规则已装（无需 sudo） |
| 固件 | **1.12.0 补丁固件**（0x28 门控 + TX 队列满上报 + TERMINATE reason 上报 + INITIATING 重试 + RX 8 深）。`probe_fw_version()` 应返回 `1.12.0`；缺补丁时时序用例与掉链 reason 判定不可用 |
| Python | python3 + `pyserial` + `pyyaml`（`pip install pyserial pyyaml`）；GUI 额外 `pip install nicegui`；离线解密需 `pycryptodome` |
| 目标 | 一个"GATT 免配对即可访问"的 BLE 外设（耳机/鼠标）；加密冒充需已从手机收割 bond 密钥 |
| 环境 | 板子必须插着；目标设备在广播状态（耳机开盖/配对模式最稳） |

---

## §1 快速开始

### 第 1 步：填目标档案

编辑 `att-fuzz/targets/headphone.json`：

```json
{
  "name": "headphone",
  "mac": "A4:C1:38:xx:xx:xx",
  "search_string": "",
  "mac_random": true,
  "conn_interval": 12,
  "latency": 0,
  "connect_timeout": 10
}
```

`mac` 和 `search_string` 至少一个非空。用广播名的好处：耳机每次重连地址可能变（RPA 轮换），按名找不会断。`mac_random` 用 `true/false` 或 `1/0` 均可；不确定时先跑 `--probe` 看实测地址类型再定。

### 第 2 步：冒烟（发现 GATT，不跑语料）

```bash
python3 att-fuzz/runner.py --target att-fuzz/targets/headphone.json --discover-only
```

预期输出：`ll_max=251 att_mtu=247`、服务列表、`GATT 地图已保存`。这一步验证串口/固件/连接/DLE/MTU/发现全链路。任何一步卡住，先看 §8 排查。

### 第 3 步：首跑（50 用例冒烟）

```bash
python3 att-fuzz/runner.py --target att-fuzz/targets/headphone.json --max-cases 50
```

跑完看 `logs/run-<时间戳>/` 下产物（§5）。分类分布正常应大量 `ERROR_RESPONSE` + 少量 `OK_RESPONSE`，几乎无 `TIMEOUT`。

---

## §2 原版 Sniffle 工具

原版工具在 `python_cli/` 下，运行方式 `python3 python_cli/<tool>.py ...`。与 fuzzer 共享 `python_cli/sniffle/` 库但独立运行。串口锁同样约束它们（同一时间只有一个任务能上机）。

### sniff_receiver.py — 主力嗅探器

捕获 BLE 广播与连接流量，输出 pcap。

```bash
python3 python_cli/sniff_receiver.py \
  -s /dev/ttyACM0 \          # 串口(默认自动探测 XDS110)
  -c 37 \                     # 广播信道(37/38/39)
  -m A4:C1:38:xx:xx:xx \      # 按 MAC 过滤(可选)
  -i <IRK_hex> \              # 按 IRK 过滤(可选,解析 RPA)
  -S "EDIFIER" \              # 按广播名片段过滤(可选)
  -o capture.pcap \           # 输出 pcap 文件
  [-a] \                       # 只收广播,不跟连接
  [-A] \                       # 主动扫描模式(发 SCAN_REQ)
  [-e] \                       # 捕获 BT5 扩展(辅助)广播
  [-H] \                       # 扩展模式下跳三个主广播信道
  [-l] \                       # 长距离(Coded PHY)模式
  [-C] \                       # 捕获 CRC 错误包
  [-d] \                       # 解码广播数据
  [-q]                         # 不显示空包
```

### scanner.py — 主动扫描器

发 SCAN_REQ，收集附近所有广播者，表格输出。

```bash
python3 python_cli/scanner.py -s /dev/ttyACM0 -c 37 [-o scan.pcap] [-d] [-l]
```

### initiator.py — 连接发起测试

作为 central 发起连接，验证目标可连接性。

```bash
python3 python_cli/initiator.py -s /dev/ttyACM0 -m A4:C1:38:xx:xx:xx [-P] [-l]
#  -P: MAC 是 public 地址(默认 random)
```

### relay_master.py + relay_slave.py — 双板 relay MITM

两块板子配合：master 控制与目标的连接，slave 广播中继。需要两台 LaunchPad。

```bash
# 终端 1(slave 板):
python3 python_cli/relay_slave.py -s /dev/ttyACM1

# 终端 2(master 板):
python3 python_cli/relay_master.py -s /dev/ttyACM0 -m A4:C1:38:xx:xx:xx \
  -o relay.pcap [-f] [-F] [-p]
#  -f: slave 请求快连接间隔
#  -F: master 指定快连接间隔
#  -p: 等按键再开始 relay
```

### advertiser.py — 广播测试

板子发广播，测试 advertising 功能。

```bash
python3 python_cli/advertiser.py -s /dev/ttyACM0
```

### pcap_decoder.py — pcap 离线解码

解析 BLE pcap 文件，打印包内容（不涉及加密解密）。

```bash
python3 python_cli/pcap_decoder.py capture.pcap [-d] [-q]
#  -d: 解码广播数据
#  -q: 不显示空包
```

### sniffle_extcap.py — Wireshark extcap 集成

Wireshark 插件，在 Wireshark 里直接选 Sniffle 作为捕获接口。不需要手动运行，Wireshark 自动调用。配置：将 `sniffle_extcap.py` 路径加入 Wireshark extcap 目录。

### reset.py — 固件复位

发送命令同步 + 5 次 `cmd_reset()`，确认固件恢复到已知状态。

```bash
python3 python_cli/reset.py -s /dev/ttyACM0
```

### version_check.py — 固件版本检查

```bash
python3 python_cli/version_check.py -s /dev/ttyACM0
```

打印固件版本字符串与 API level。API level 不匹配会告警。

---

## §3 Fuzzer CLI 全量参考

```
python3 att-fuzz/runner.py \
  --target att-fuzz/targets/headphone.json \
  [--strategy att-fuzz/strategies]        # 策略目录/单个 yaml,默认全目录
  [--seed 1]                              # random 值内容的确定性种子
  [--max-cases 0]                         # 0=全量(默认)
  [--outdir logs/run-xxx]                 # 输出目录(默认 logs/run-<时间戳>)
  [--serport /dev/ttyACM0]                # 串口(默认自动探测 XDS110)
  [--rounds 0]                             # 变异轮数(0=纯确定性;N>0 签名驱动变异)
  [--round-budget 100]                    # 每轮变异预算用例数
  [--discover-only]                       # 只连接+发现,不跑语料
  [--probe]                               # 扫描诊断:目标是否在广播+地址类型(不连接)
  [--replay <PDU_HEX>]                    # 重放指定 ATT PDU
  [--replay-case <CASE_ID>]               # 从台账按 case_id 找 PDU 重放
  [--ledger <ledger.jsonl>]               # replay-case 用的台账(默认最新 run)
  [-v]                                    # DEBUG 日志
```

### 模式 1：直连 Fuzz（默认）

免配对直连 → GATT 发现 → 确定性语料 → 可选变异轮。

```bash
# 全量语料(约几百用例)
python3 att-fuzz/runner.py --target att-fuzz/targets/headphone.json

# 只跑某层
python3 att-fuzz/runner.py --target ... --strategy att-fuzz/strategies/offsets.yaml

# 变异轮:第一轮确定性 + 3 轮签名驱动变异(每轮 100 条预算)
python3 att-fuzz/runner.py --target ... --rounds 3 --round-budget 100

# 崩溃复现
python3 att-fuzz/runner.py --target ... --replay 0a0000
python3 att-fuzz/runner.py --target ... --replay-case h-read-0000 --ledger logs/run-xxx/ledger.jsonl
```

### 模式 2：加密冒充（`--impersonate`）

加载 bond 密钥 → 冒充手机地址 → LL_ENC 握手 → 加密链路 GATT 发现 → 认证面语料 → ATT_FREEZE 触发重连续跑。

```bash
python3 att-fuzz/runner.py \
  --target att-fuzz/targets/vivo_tws.json \
  --impersonate \
  --bt-keys bt_keys/vivo.conf \          # 相对路径(解析到 targets/ 下),或绝对路径
  --keys-mac 64:44:7B:EE:41:F4 \         # bt_config 里耳机 MAC(书写序);留空时回退 target.mac
  --phone-mac 00:C3:0A:02:6C:24 \        # 手机 public MAC(书写序),冒充此地址
  --strategy att-fuzz/strategies/ \
  --max-cases 50 \
  --imp-duration 0 \                      # 0=跑到 Ctrl-C
  --wall-ledger logs/run-xxx/ledger.jsonl  # 阶段一台账(可选),供 0x05 墙 handle 加载
```

bt_keys/keys_mac/phone_mac 也可以写进 target JSON，CLI 会自动回退读取，命令行只传 `--target` 和 `--impersonate` 即可。密钥文件放 `targets/bt_keys/` 下用相对路径引用。

产物：`logs/run-<ts>/` 含 `fuzz_ledger.jsonl`（语料分类台账）+ `impersonation_ledger.jsonl`（握手/连接/墙验证事件）+ `capture.pcap` + `gatt_enc.json`。

### 模式 3：反向角色（`--server`）

板子广播可连接 peripheral，手机连入后对手机的 ATT 请求回响应，logcat oracle 检查手机侧崩溃。

```bash
python3 att-fuzz/runner.py --target att-fuzz/targets/xxx.json --server \
  --server-name "Sniffle Server" \
  --server-duration 300 \
  --adb-serial ZD9L8H454HDY7DEU
#  --server-duration 0 = 一直跑到 Ctrl-C(默认)
#  --adb-serial:logcat oracle 目标手机序列号;adb 缺失则 oracle fail-open
#  产物: capture.pcap + transport.jsonl + server_ledger.jsonl + crashes/
```

### 模式 4：被动嗅探配对（`--sniff-pairing`）

嗅探 SMP 交换 → legacy 密钥推导 → 加密会话解密。

```bash
python3 att-fuzz/runner.py \
  --sniff-pairing \
  --sniff-mac 64:44:7B:EE:41:F4 \        # 目标外设 MAC(书写序);缺省=猎取模式(无 MAC 过滤+extadv)
  --phone-mac 00:C3:0A:02:6C:24 \        # 手机 MAC(书写序);用于标记手机发起的配对连接
  --sniff-duration 0 \                    # 0=跑到 Ctrl-C
  [--sniff-hold]                          # 跟满整条连接(禁用 60s 无 SMP 超时复位;加密重连会话收割用)
```

### 模式 5：离线 pcap 解密（`--decrypt`）

加密 BLE pcap + LTK → ATT/SMP 明文 SDU。不碰硬件。

```bash
# 用 bt_config.conf 或提取 JSON 做密钥源
python3 att-fuzz/runner.py \
  --decrypt capture.pcap \
  --bt-keys bt_keys/vivo.conf \          # 相对路径或绝对路径
  --keys-mac 64:44:7B:EE:41:F4           # bt_config 里目标 MAC(可选,缺省解析全部节)

# 或直接给 LTK(16 字节 hex)
python3 att-fuzz/runner.py --decrypt capture.pcap --ltk a89469ab06f390362c32a49ac1d392c1
```

LTK 字节序：bt_config dump 序（小端）与空口/大端序相反。引擎自动双序尝试、MIC 裁定。产物：`logs/decrypt-<ts>/` 含 `decrypt_report.json` + `decrypted_sdu.jsonl`。

---

## §4 攻击面与语料

| 层 | 文件 | 内容 |
|---|---|---|
| ① opcode | `strategies/opcodes.yaml` | 保留 opcode、合法\|0x40/0x80 翻转、0xE0-0xFF 原始注入 |
| ② handle | `strategies/handles.yaml` | 0x0000/0xFFFF/0xFFFE/0x8000、真实 handle ±1、声明/CCCD handle、发现类 start>end/全0/全1 |
| ③ value | `strategies/values.yaml` | 长度 0/1/20/MTU-3/MTU-2/MTU-1/MTU/MTU+5/244/247/255 × 内容模式 |
| ④ offset | `strategies/offsets.yaml` | 0/1/baseline_len±1、0x7FFF/0x8000/0xFFFF，read_blob 与 prepare_write 双打 |
| ⑤ MTU 协商 | `strategies/state_machine.yaml` | 未协商 MTU / 重协商到极小值（稳定损害 29/30+1 静默死） |
| ⑥ Prepare 队列 | `strategies/prepare_execute.yaml` | 队列深度灌满（≈22/属性）、空队列 Execute、混合 offset、非法 flags |
| ⑦ CCCD | `strategies/cccd.yaml` | 无 2902 特征写 0x0100（不校验 notify 能力）、值集单发 |
| ⑧ 发现类 | `strategies/discovery.yaml` + `strategies/l2cap.yaml` | Read By Group/Type 越界、L2CAP 帧头欺骗/逃逸口注入 |

**语料锚点语法**（写自己的策略时用）：

```yaml
- id: my-case-1
  layer: value
  op: write_req
  handle: "${each.value}"        # 按每个特征展开(结合 filter)
  value: {len: "${mtu-3}", pattern: incremental}
  filter: writable               # 只对含 write 属性的特征展开;readable 同理
```

- `${each.value}` 值 handle、`${each.decl}` 声明 handle、`${each.cccd}` CCCD、`${each.baseline_len}` 基线值长度
- `${mtu}` 协商 MTU、`${mtu-3}`、`${baseline_len+1}` 算术
- `value.pattern`: `zero / ff / incremental / random / fmtstring / ascii`
- op 全集：`read_req / read_blob_req / write_req / write_cmd / prepare_write_req / execute_write_req / find_info_req / read_by_type_req / read_by_group_type_req / read_multiple_req / exchange_mtu_req`
- 原始注入：`{opcode: 0x00, payload: "0100"}`（不解析参数直接发）

**变异引擎**（`--rounds N`）：第一轮跑确定性语料；之后 N 轮从签名库（`logs/signatures.json`）抽取"新签名"种子，用 4 类算子（位翻转/字节插入/值替换/gate_at 时序门控）变异，能量加权（告警层优先）。同 seed 同 GATT 地图 → 变异用例逐字节确定。

---

## §5 输出产物（logs/run-<时间戳>/）

| 文件 | 内容 | 用途 |
|---|---|---|
| `ledger.jsonl` | 直连 fuzz 每用例一行 | 判定与复现的源头 |
| `fuzz_ledger.jsonl` | 加密冒充语料台账（与 ledger.jsonl 格式一致） | 加密链路 fuzz 结果 |
| `impersonation_ledger.jsonl` | 冒充事件流（bond_loaded/enc_engaged/conn_start/link_drop/corpus_done 等） | 握手与连接级排障 |
| `server_ledger.jsonl` | 反向角色"手机请求→我方响应"对 | server 会话审计 |
| `capture.pcap` | 全量 LL 无线包（含用例边界 marker 包） | Wireshark 打开（DLT 256） |
| `transport.jsonl` | 传输层原始事件 | 排查传输层问题 |
| `gatt_map.json` | 直连发现的服务/特征/CCCD/基线 | 下次可直接复用 |
| `gatt_enc.json` | 加密链路发现的 GATT 地图 | 加密冒充会话产物 |
| `crashes/` | logcat oracle 命中的崩溃窗口（反向角色模式） | 手机侧崩溃证据 |

**分类含义**：

| 分类 | 含义 | 动作 |
|---|---|---|
| `OK_RESPONSE` | 正常（非错误）响应 / 无响应类用例完成 | 无 |
| `ERROR_RESPONSE` | ATT Error Response，带 error_code | 无 |
| `TIMEOUT` | 超时无响应 | 关注：可能目标栈忙/异常 |
| `DISCONNECT_TERM` | 目标发 TERMINATE，带 reason 码。**reason=0x08 是"栈崩了"的强信号** | 立即复现 |
| `DISCONNECT_SUP` | 目标静默（supervision timeout） | 复现 |
| `TX_QUEUE_FULL` | 传输层错误（队列满/未连接），用例无效 | 重跑该用例 |
| `HEALTH_DEGRADED` | 用例"完成"但后置健康检查异常 | 重点看 |
| `ATT_FREEZE` | ATT 层冻结：post-HC 健康检查读无响应，但 LL 链路存活；重连即恢复 | 立即复现——最高价值信号之一 |

**崩溃分析流程**：

```bash
# 1. 看分类分布
python3 -c "
import json
c={}
for l in open('logs/run-xxx/ledger.jsonl'):
    r=json.loads(l); c[r['classification']]=c.get(r['classification'],0)+1
print(c)"

# 2. 复现单条(自动从台账取 PDU)
python3 att-fuzz/runner.py --target ... --replay-case <case_id> --ledger logs/run-xxx/ledger.jsonl

# 3. 复现成功 → 保留 pcap,用 marker 时间定位帧,提取最小化
# 4. 复现失败/偶发 → 同一用例多连几次,关注前后健康检查字段
```

---

## §6 GUI 可视化控制台（NiceGUI）

> 跨平台，浏览器渲染（可 `--native` 原生窗口），与 CLI 共享同一套 core。**串口按任务懒占用**：GUI 空闲时不碰串口，CLI 可正常使用；只有点了探测/发现/Fuzz/冒充/广播才打开串口，任务结束自动释放。

### 启动

```bash
pip install nicegui                       # 依赖
python3 att-fuzz/gui/app.py               # 浏览器模式 http://localhost:8765
python3 att-fuzz/gui/app.py --native      # 原生窗口(需 pip install pywebview)
python3 att-fuzz/gui/app.py --port 9000 --host 0.0.0.0   # 局域网远程访问
python3 att-fuzz/gui_demo.py              # 离线演示:FakeHw 模拟,无硬件
```

### 控制台页

- **模式选择器**：直连 Fuzz / 加密冒充 / 反向角色（toggle 切换，参数区跟着变）
- **冒充参数区**：imp-duration 输入框；bt_keys / keys_mac / phone_mac / wall_ledger 全从 target JSON 读取（在上方档案编辑器填写），GUI 不显示输入框
- **策略复选框**横排 + seed / max-cases / rounds / round-budget 横排
- 探测广播 / 仅发现 GATT / 开始按钮（标签随模式切换：开始 Fuzz / 开始冒充 / 开始广播）
- 暂停 / 继续 / 停止 + 进度条 + GATT 地图树 + 运行日志

### 实时仪表盘

- 进度/速率/用时统计卡、分类分布环形图、累计用例曲线
- 连接健康 chips：串口 / 链路 / 连接事件 / ATT MTU / LL max / TX 队列满 / 重连 / **加密**（明文/加密）/ **ATT 冻结**计数 / 固件
- 告警列表 + 传输层事件流（可按 kind 过滤，含 enc_engaged/corpus_done/link_drop 等新 kind）

### 结果与重放

- 多台账自动探测（`find_ledger`：ledger.jsonl > fuzz_ledger.jsonl > impersonation_ledger.jsonl > server_ledger.jsonl）
- 台账表格（按分类/攻击层/关键词过滤），点行看详情
- 一键 Replay ×1/×5（仅直连 fuzz 台账 `ledger.jsonl` 可重放；加密冒充/反向角色台账的 replay 按钮禁用 + tooltip 提示）
- 导出 Markdown 摘要

### 历史会话

- `logs/run-*` 列表，勾选两个会话做分类分布对比图
- 单会话摘要导出

### target JSON 模板

新建档案时模板含 `bt_keys`/`keys_mac`/`phone_mac` 可选字段 + 中文说明。加密冒充目标需在 JSON 填 `phone_mac` + `bt_keys` 路径，密钥文件放 `targets/bt_keys/` 下用相对路径引用。

---

## §7 维护与扩展

### 离线测试（不需要板子，改动后必跑）

```bash
python3 att-fuzz/tests/test_offline.py   # 编解码/L2CAP 重组/语料展开/确定性/加密/冒充握手/多台账
python3 att-fuzz/tests/test_dryrun.py    # FakeHw 模拟 GATT server + 加密冒充 + controller,走通全流程
```

### 加一层语料

在 `strategies/` 加 yaml（见 §4 语法）。展开规模 = 模板数 × 命中特征数，注意别把 `${each.*}` 配到不需要的 `filter` 上把语料撑爆。

### 换目标设备

复制 `targets/headphone.json` 成 `targets/<设备>.json`，改 MAC/广播名/连接参数。语料会自动按发现结果展开（锚点来自运行时 GATT 地图，不写死）。

### 冒充目标

在 target JSON 额外填：
- `phone_mac`：手机 public MAC（书写序），加密冒充冒充此地址
- `bt_keys`：密钥文件相对路径（如 `bt_keys/vivo.conf`），放 `targets/bt_keys/` 下
- `keys_mac`：bt_config 里目标设备 MAC（可选，99% 与 `mac` 相同，留空自动用 `mac`）

密钥文件从手机 root 提取 `bt_config.conf`（`/data/misc/bluedroid/bt_config.conf`）或手工归档为 JSON（含 `ltk_hex`/`rand_hex`/`ediv_hex` 字段）。

---

## §8 常见问题排查

| 现象 | 原因与处理 |
|---|---|
| `Sniffle device not found` | 板子没插/udev 规则没生效。`ls /dev/ttyACM*`，`--serport` 手动指；不行就重插 |
| 连接永远失败 + 日志里完全看不到目标广播 | **MAC 字节序**：固件全线用线序（小端）。transport 的 `_parse_mac` 已自动反转，但自写脚本直接调 `cmd_mac`/`initiate_conn` 必须传线序（`64:44:7B:EE:41:F4` → `F4 41 EE 7B 44 64`） |
| 目标广播正常但连接失败（0x1408） | 嘈杂环境下 initiator 被干扰；固件 1.12.0 补丁（INITIATING 重试 + 8 深 RX 队列）已处理。确认固件版本，且 `cmd_auxadv(True)` |
| vivo TWS 耳机盒内闲置后连不上 | 设备节电：广播照发但 LL 不 ACK。开盖激活即可 |
| `connection timed out` 后板子无响应 | host 在 INITIATING 中途退出会让固件卡死，transport 已自动 cmd_reset 自愈 |
| 台账大量 `TIMEOUT` | 目标反应慢，把 `conn_interval` 调大（如 24）降速 |
| `HEALTH_DEGRADED` 高频 | 健康检查读的值本身就老变（动态特征）。`known_good_handle` 优先 Device Name |
| GATT 地图与 nRF Connect 对不上 | 先看 `gatt_map.json` 的 `gaps`；很多耳机有动态服务，重发现一次 |
| **加密冒充握手失败**（ENC_RSP 超时） | 密钥不匹配或 bond 过期。重新从手机提取 `bt_config.conf`，确认 LTK 未因重配对变更 |
| **ATT_FREEZE 复现** | 连接后无害读预热到 event ~1100 会触发冻结（每连接 ATT 状态死锁，LL 存活）；重连即恢复 |
| **pcap 解密 MIC 全失败** | LTK 字节序：bt_config dump 序（小端）需整体反转才是密码学大端序。引擎自动双序尝试，若全失败则密钥不匹配或 bond 已过期 |
| **GUI 冒充模式缺参数** | 在 target JSON 补 `bt_keys`（密钥文件路径）+ `phone_mac`（手机 MAC）字段；密钥文件放 `targets/bt_keys/` 下 |

---

## §9 当前状态

见《进度.md》——状态类信息只在那一处维护，本文不再重复。
