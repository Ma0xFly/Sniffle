# Sniffle ATT/GATT Fuzzer 进度

> **当前所处:阶段四 4.1 代码交付 + 离线验证通过,真机配对捕获被 vivo 配对机制阻断**(2026-08-31)。鼠标 bring-up 已按有限形态收口。阶段定义与验收标准见《设计.md》§八。
> 新会话接手:先读本文件 → 需要理解设计再读《设计.md》→ 要跑起来看《使用指南.md》。
> 更新约定:每完成一个功能/发现重要事实,更新对应小节并在文末追加一行。

## 当前状态(2026-08-30 深夜)

**已完成(阶段一全部收尾,APM Stage 1)**:
- 阶段一四层语料(opcode/handle/value/offset),模板展开 → PDU 去重后 572 条
- 固件 1.12.0 补丁烧录生效(0x28 门控/TX 满上报/TERMINATE reason/INITIATING 重试/RX 8 深),分支 att-fuzz-fw
- CLI 全套:probe / discover-only / fuzz / replay / replay-case
- GUI(NiceGUI)四页面 + 目标档案管理 + 串口锁 + 日志持久化;**GUI fuzz/发现/重放现已产出 capture.pcap(与 CLI 一致,probe 除外)**
- 历史页已排除自测产物 dryrun-test;死代码 state.take_logs/_log_read 已删
- **仓库已入库**:基线 att-fuzz-fw(bootstrap 四提交 + docs + Stage 1 合流 3054aea);git 身份 myx/myx@local(占位,可换)
- **变异引擎(2.1)**:core/mutator.py(跨 run 签名库 logs/signatures.json + 能量加权抽样 + 4 类算子含 gate_at 时序门控/同事件多发);runner --rounds N/--round-budget;多 PDU 门控 replay;同 seed 变异逐字节确定
- **ATT_FREEZE 独立分类(2.6)**:post-HC 读无响应+链路存活 -> ATT_FREEZE(与"有响应但异常"的 HEALTH_DEGRADED 分离);GUI/指南同步;历史台账不重分类

**告警定性结论(17 条全部闭环,85 次硬件重放,产物 logs/repro-20260830-230311/)**:
- **op-\* 组 8 条(未知/保留/gap/vendor opcode + 合法 opcode 置 0x40 高位 0x4A/0x42/0x50)5/5 稳定复现**:目标**纯静默**——无 ATT Error、无响应,但栈存活可继续服务。定性 = 协议合规/健壮性缺陷(蓝牙规范建议至少忽略后保持响应语义),亦是栈指纹线索;非崩溃非 DoS
- **HD 组 9 条 0/5 不可复现**:新鲜连接下目标均正常响应(o-prepare-ffff@001d → 干净 Error 0x03;v-len-mtu-incr@0018 → Error 0x04;v-len-20-incr@0011 → Error 0x0D 非标但无害)
- **原"约 8 次静默掉链"重新定性(重要)**:不是 LL 掉链/supervision timeout——transport.jsonl 显示 9 次重连全部紧跟 9 条 HD 用例,重连前最后注入是健康检查读(无响应),全程 0 次 TERMINATE。真象 = **目标 ATT 层冻结(链路存活)+ host 主动重连**,仅现于长会话深处(event 988–1064,第 10 连接段)。定性精化:**每连接 ATT 状态死锁**(冻结期 LL 存活=目标未重启,重连即恢复);最小复现路径=序列最小化(单包重放 0/5);根因是否内存破坏待祭品板(阶段五)。**追查 HD 需"长会话+用例序列"设计**(阶段二 2.2 序列型用例正是)
- **0x05 权限墙(免配对面)**:663 条中 206 条 Insufficient Authentication(offset 层 133/value 45/handle 28)——权限拓扑免费测绘完成;这批被拒 handle 清单 = **阶段四 relay 的优先目标地图**;策略 = 一二阶段不碰墙,先消化免配对面(冻结/静默两类信号全在墙之前暴露)

**⑥⑦层实测(2026-08-31,Task 2.3/2.4,产物 logs/run-20260831-*)**:
- **Prepare 队列深度黑盒测量 ≈22/属性**(跨 handle 全局 ~28 上限);打满返回 **0x11 Insufficient Resources 而非规范 0x09 Prepare Queue Full(合规偏差,可报告)**;空队列 Execute -> 0x0D;非法 flags -> 0x06(合规);混合 offset 提交 -> 0x07;**Prepare 接受但 commit 拒绝 0x13(状态机不一致线索)**
- **ATT 冻结决定性复现**:长会话(event 978–1072)7×HD+8×TIMEOUT 聚集,与阶段一冻结区间(988–1064)重合;`pe-hunt-fill-commit` 灌满队列后读探针超时在长会话复现、隔离单跑不复现--**最小触发集 = 长会话累积(≥~900 event)+ 队列灌满压力**(逐条裁剪待后续);追查方向 = "长会话预热 + 队列灌满"序列
- **CCCD:vivo 无已发现 0x2902**;6/14 可写特征**不校验 notify 能力接受 0x0100**(合规偏差,接受后无通知发出);值集单发撞只读墙全 0x03(值级语义需打在可写特征上)
- harness 加固:空口残包不再打崩连接流程(解码兜底);固件复位后串口重开失败改重试+可控报错(原裸崩中断 410 条长跑);test_dryrun 不再依赖真实串口空闲

**L2CAP 帧欺骗 + 语料补强实测(2026-08-31,Task 2.5,产物 logs/run-20260831-10{3911,3948}/)**:
- **vivo L2CAP 重组器容错良好**:超声明/少声明/半截 SDU/只续条均"丢弃不响应但 post-HC ok"(无 ATT_FREEZE/无掉链)重组有清理路径,不挂起等待--与 ATT 冻结形成对照(单用例无响应但健康检查正常),此攻击面排除
- 发现类区间边界(全区间/全 1/跨界)+ Read Multiple 句柄表边界(空/重复/奇数)15 条全合规(8 ERROR/7 OK,零告警)
- transport 新增 inject_raw 逃逸口(raw LL 帧+可选门控);9 帧灌包触发发端 _wait_tx_room 限速(TX_QUEUE_FULL=发端边界非目标信号),灌包类用例注意帧节奏

**xisem 鼠标 bring-up 结论(2026-08-31,Task 2.8,有限形态收口)**:
- **工具链目标无关性验证通过**:同一套 127 模板语料在鼠标 GATT 地图(5 服务/20 特征/5 CCCD)自动展开 494 条(vs vivo 721),换目标零代码改动
- **首个"写入真实改变设备状态"数据点**:写 Generic Access 0x2A00 Device Name 被接受,鼠标名被清空(可写面比预期广);对只读 Battery 写 CCCD 值 -> HEALTH_DEGRADED
- **深 fuzz 被目标连接策略阻断**:被 PC 信任的 HID 外设仅配对窗口可连(随机地址每次变、窗口极短、退出只对 PC 定向广播、连上即断);Beken 芯片对未加密会话防御远强于 vivo。鼠标记为"有限档案目标"(仅 bring-up;深挖需 SMP=违背设计边界,或换稳定目标,或阶段四 relay)
- GATT 画像:HID 1812 + 私有 FFF0(0xfff1 手册称可写"黑匣子")+ 5 真实 CCCD;不支持 DLE(ll_max=27)

**server_fuzz 应答核 + logcat oracle(2026-08-31,Task 3.1,阶段三第一刀)**:
- **反向角色骨架落地**:`roles/server_fuzz.py`——Sniffle 以可连接 peripheral 广播(完整本地名 + Battery 0x180F 诱导服务发现),等手机 CONNECT_IND 连入,对手机 ATT 请求逐条回可配置响应;断链后自动重新广播(循环)
- **ATT server 应答引擎**(`core/att_server.py`):伪 GATT 数据库(4 服务/7 特征/1 CCCD,含可写私有 0xFF01)全 16 位 UUID、句柄连续;**响应生成器与数据库解耦**(每请求类型一个生成器函数,策略参数经 policy 传入)——3.2 恶意策略 = 替换生成器行为,应答循环零改动。正常模式覆盖 exchange_mtu/find_info/find_by_type_value/read_by_type/read_by_group_type/read/read_blob/read_multiple/write/prepare/execute,回合法响应;未知 opcode 回 0x06、畸形参数回 0x04、只读/只写权限面按 spec 回 0x02/0x03
- **logcat oracle**(`core/adb_oracle.py`):两段式——`CrashDetector` 纯逻辑(8 条蓝牙崩溃/重启特征正则,自检全命中+良性零误报),`AdbOracle` 薄包装(adb 子进程后台喂行,命中落盘 outdir/crashes/ + 事件携带归因 context)。adb 缺失/手机未连 **fail-open** 不影响主循环;`--selfcheck`/`--replay-file` 离线自检路径可用
- **transport 增量**(默认 central 零行为变化):`role` 属性 + `advertise()`(照 relay_slave 序列)+ `accept_connection()`(从 CONNECT_IND 取 aa/连接参数,对齐 cur_aa)+ 连接态判定按角色(peripheral 以 PERIPHERAL 为连上);pcap TX 方向按角色标 pdu_type 2/3
- **台账**:`server_ledger.jsonl` 记"手机请求 → 我方响应"对(连接号+请求序号+opcode+att_mtu),服务发现序列是后续最小化崩溃的证据链;oracle 崩溃事件带 context 落同台账
- **硬件+手机实测通过(2026-08-31,产物 logs/run-server3-3/)**:nRF Connect 连上广播 → LL 握手走通(FEATURE/VERSION 交换)→ 完整 GATT 服务发现 24 条请求被正确应答(Read By Group Type×6 逐段返回 4 服务 / Read By Type×16 特征声明 / Find Info CCCD / Read)→ 手机可见全部 4 个假服务(Generic Access/Battery/Device Info/FFF0);oracle 真机流命中 Watchdog bt 进程重启信号并落盘
- **真机逼出三个只在真实栈暴露的 bug(均已修+离线测试全过)**:
  1. `transport.LL_UNKNOWN_RSP` 常量错标(0x08 实为 LL_FEATURE_REQ)——FEATURE_REQ 被误解析为 unknown_rsp(806a9d5 前就存在)
  2. **固件不答 LL_VERSION_IND → LL 控制过程在 MTK 栈串行挂起 → DLE/ATT 永不开始 → GATT 40s LMP 超时**(前几轮 0 条 ATT 的根因);host 代答 VERSION_IND + CONNECTION_PARAM_REQ 后走通(b5da932 修 FEATURE 后仍未通,7dc032d 补 VERSION/PARAM)
  3. **重广播循环每轮 `random_addr()` 换 MAC** → 手机缓存地址失配、"连不上";改为启动时生成一次稳定地址全程复用(7dc032d)
  - 另:server_fuzz 遗留 UnboundLocalError(旧 staticmethod 残留)导致连上后即崩(806a9d5);oracle 子进程流式解码 UTF-8 崩 + bt_stack/tombstone 裸 TAG 误报风暴 308 次(806a9d5 修)
- 实测:两套离线测试全过(含新 server_fuzz FakeHw 广播/接受连接/应答循环段 + 应答引擎 25 项字节级断言 + oracle 自检);central 路径回归不变

**ATT 冻结手册级结论(2026-08-31,Task 2.7,Stage 2 核心成果)**:
- **触发条件 = 单连接 ATT 交互累积超阈值 event ~1100**(约 880 次读;连接+发现耗 ~220 event),与输入类型无关--纯无害 read_req 循环 3/3 复现,阈值 1102/1103/1103(±1),像内部计数器/缓冲上限;早前"队列压力"假设被修正(pe-hunt 单独不复现只因深度不够)
- **行为学**:≥300s 不自愈;冻结后 ~60s LL supervision 断链(两段式:ATT 先死 LL 后断);断链后广播重现(未死机),重连即恢复
- **报告定性**:免认证、可复现的连接级 GATT 服务 DoS(持续交互 3-5 分钟即触发,无需恶意 PDU);音频(经典蓝牙)与整机无损;低-中危。复现:连接后无害读预热到 event ~1100(脚本逻辑见 task-02-07,3/3)
- 长跑触发统计:4 组 run,ATT_FREEZE 集中 event 969–1103,用例级触发率 1-2%(超阈值后显著上升)

**变异引擎实测(2026-08-31,Task 2.1,产物 logs/run-20260831-10{2107,2549}/)**:
- **ATT_FREEZE 真机首捕 4 次**(变异轮长会话,cccd 层写序列后 post-HC 无响应、链路存活、重连恢复);cc-jitter-seq@0037 从 HEALTH_DEGRADED 正确归位--2.6 分类细化的直接实证
- 能量调度端到端生效:变异告警 20/20 集中在历史告警层(cccd);无种子时变异轮正确不空转("no seeds, stop" 负例)
- 过程修复:种子 PDU 误取台账响应(0x13)而非请求(0x12)的 bug--变异语义回归"在真实输入周围"

**⑤层实测(2026-08-31,Task 2.2,产物 logs/run-20260831-*)**:
- MTU 协商滥用 = 合规拒绝:已协商链路上任何二次 Exchange MTU 一律 0x04 Request Not Supported,无损害(vivo 严格执行一连接一协商)
- **未协商 MTU 即读写 = 稳定健康损害(当前最强可报告信号)**:30 条非协商用例 29 条 HEALTH_DEGRADED 逐用例累积,含 1 次 DISCONNECT_SUP 静默死;run1 出现 ATT 全静默+重连后 0 服务(同阶段一深会话冻结特征),目标事后自愈。最小复现=连接后跳过 Exchange MTU 直接 Read Blob(sm-noneneg-blob-long,三轮一致)
- **冻结影响域修正(2026-08-31 用户实测)**:冻结触发时音频播放不受影响--TWS 音频走经典蓝牙 A2DP,与 BLE GATT 独立;冻结真实影响域 = **每连接 GATT 服务 DoS**(重连即恢复,主功能无损),此前"栈冻结"定性过冲。决定性补充实验:冻结后断开,正主手机 App 连接是否正常(2.7 行为学 + 用户手动实测)
- **功能服务未触发的原因(设计取舍,非 bug)**:暂停/播放走 AVRCP/触摸传感器不在 GATT;切换模式等 GATT 功能是厂商私有协议帧(带 magic/校验),边界值语料写入被接受但解析层丢弃;敏感特征多在 0x05 认证墙后。语义语料 = 阶段四 relay 嗅探真实 App 会话后解决;行为学 oracle(§七 tier2/3)未实现是已知盲区
- 冻结怪癖补充:冻结后放回盒内/等待 supervision timeout 自愈(三次实测 probe 均恢复)
- harness 修正:恢复期 GATT 重发现不再替换完好缓存地图(残缺/空发现只记日志)——归因污染修复,对全部 fuzz 路径生效

**技术债 / 已知缺口(更新)**:
- ~~replay 忽略台账 `replay.expect_response`~~(Task 2.2 已修,带回归测试)
- 连续重放间 13/99 瞬态 "not connected"(重试即恢复,疑耳机端清理时序);CLI replay 退出不断链,连续脚本需 cmd_reset() 清残留
- PcapBleWriter(python_cli/sniffle/pcap.py)无 close/flush,GUI 以 .output.close() 防御性规避
- ~~GUI _make_transport pcap=None~~(已修) ~~dryrun-test 混历史页~~(已排除) ~~state.take_logs~~(已删) ~~docs 未进 git~~(已入库)
- strategies 展开数口径:663 = 18:18 实跑时未提交工作树的展开数,已提交 strategies 在 181858 地图上展开去重后 572——引用统计时注意口径

## 下一步(按优先级)

1. **阶段四 4.1 真机配对捕获(被 vivo 配对机制阻断,待解)**:代码就绪但嗅探器没锁定配对连接(三轮实测含清手机 bond 缓存后均 0 CONNECT_IND/0 SMP 帧)。需 Manager 决策:① 开 ext_adv 嗅探扩展广播;② 换标准 BLE 配对目标;③ 走 4.4 relay 双板主动 MITM(LE SC 也需此)
2. 4.2 语义语料收割(解密真实 App 会话提取厂商命令语言)/ 4.3 认证面注入(LTK 冒充手机,打 0x05 墙后面)/ 4.4 relay 双板(仅 LE SC 时必需,需第二块 LaunchPad)
3. 变异轮扩量实测(过夜长跑)待目标状态允许后安排
4. 阶段三 3.2 恶意响应实测:Deferred(3.1 基础设施与 oracle 已就绪,代码在 att-fuzz/server-fuzz-attack 分支 7ee597a)

## 开发纪律

- 改 core 后必跑:`python3 att-fuzz/tests/test_offline.py` + `test_dryrun.py`
- 板子:CC2652R1F LaunchPad,串口 /dev/ttyACM0(数据口);GUI/CLI 靠 serial_lock 互斥
- MAC 用线序(小端)!`64:44:7B:EE:41:F4` → `f441ee7b4464`(transport 已处理,自写脚本注意)
- 目标免配对才可直连;需配对等 relay 模式(阶段四)
- 健康检查锚点=设备名 0x2A00;动态值特征会误报 HEALTH_DEGRADED
- 连续重放/长会话脚本:每次运行后 `SniffleHW.cmd_reset()` 清残留链路(CLI replay 退出不断链),间隔 3s

## 更新记录

- 2026-08-31(Task 4.1 代码交付 + 真机受阻):被动嗅探角色(roles/pairing_sniff.py)+ SMP 解析(core/smp.py)+ BLE 密码学原语(core/bt_crypto.py:e/s1/c1/会话密钥/AES-CCM 逐包解密);**逐行核对 crackle+kernel smp.c,全向量验证通过**(BT spec CCM 样本 + crackle 01_crack 实采: c1 Mconfirm/Sconfirm、s1 STK、会话密钥、CCM 解密 LTK 分发帧、LLCipherState 端到端 3 包逐包解密,全部字节级匹配);runner --sniff-pairing 接入;两套离线测试全过。**真机配对捕获被 vivo 配对机制阻断**:三轮真机嗅探(含清手机侧 bond 缓存后)均 0 个 CONNECT_IND、0 个 SMP 帧--嗅探器压根没锁定配对连接(不是"加密太快",是 CONNECT_IND 都没抓到);根因疑:vivo 配对走快连+极短配对连接(可能扩展/定向广播,legacy hop3+MAC 过滤不跟随),或手机主动连接时机与 hop3 错开;另发现 pairing_sniff dispatch bug(连接进了 pcap 但实时台账未记)。突破路线:① 开 ext_adv 嗅探扩展广播;② 4.4 relay 双板主动 MITM(LE SC 也需此);③ 换标准 BLE 配对目标。状态 Partial。提交 546c685
- 2026-08-31(用户决策):阶段三挂起(手机侧攻击暂缓),阶段四重定义为"密钥收割优先"路线
- 2026-08-31(Task 3.1 实测收口):**硬件+手机端到端验证通过**(nRF Connect 连上 -> LL 握手 -> 24 条 GATT 发现请求被正确应答 -> 4 个假服务可见;oracle 真机命中落盘);真机逼出 4 个 bug 全修(UNKNOWN_RSP 常量错/固件不答 VERSION_IND 挂死 GATT/random_addr 每轮换 MAC/oracle 误报风暴),提交 806a9d5/b5da932/7dc032d
- 2026-08-31(Task 3.1 代码交付):server_fuzz 应答核(roles/server_fuzz.py + core/att_server.py)+ adb logcat oracle(core/adb_oracle.py)+ transport peripheral 角色增量;两套离线测试全过,central 回归不变
- 2026-08-31(Task 2.8 收口):鼠标 bring-up 有限形态关闭(工具链目标无关性验证通过;Device Name 可写;深 fuzz 被连接策略阻断);阶段三 3.1 派发
- 2026-08-31:阶段三任务定义落 Plan(3.1/3.2),用户手机+logcat 环境就绪;排队在鼠标 bring-up(Task 2.8)之后
- 2026-08-31(Task 2.7,阶段二收官):ATT 冻结手册级结论(交互计数阈值 ~1100,纯无害读 3/3;两段式;不自愈可恢复);阶段二 7 任务全部完成
- 2026-08-31(Task 2.5):inject_raw 逃逸口 + L2CAP 欺骗/发现类补强语料;vivo L2CAP 重组容错良好(负结论,攻击面排除);新增 Plan Task 2.7 冻结深挖
- 2026-08-31(上午,Task 2.6/2.1):ATT_FREEZE 独立分类落地;变异引擎落地并真机捕获 ATT_FREEZE 4 次;能量调度验证生效(20/20 集中告警层)
- 2026-08-31(上午,Task 2.3/2.4):⑥层实测(队列深度≈22/属性、0x11 非 0x09 合规偏差、prepare 接受 commit 拒 0x13);**ATT 冻结长会话决定性复现**(event 区间重合,pe-hunt 隔离不复现);⑦层实测(vivo 无 CCCD,6/14 不校验 notify 能力);harness 加固(解码兜底/串口重开重试)
- 2026-08-31:设计.md §七增补"ATT 层冻结"独立信号类(用户批准,严重度阶梯=纯静默<ATT冻结<LL静默死);Plan 增 Task 2.6 分类细化(ATT_FREEZE)
- 2026-08-31(凌晨,Task 2.2):序列型用例能力(corpus/session/runner+台账扩展+序列 replay)+⑤层实测(未协商 MTU=稳定损害 29/30+1 静默死;MTU 重协商=合规 0x04);replay expect_response 修复;恢复期重发现污染修复(保留完好缓存)
- 2026-08-31:0x05 权限墙测绘(206 条 = 阶段四 relay 目标地图);冻结精化为"每连接 ATT 死锁"(LL 存活未重启,重连恢复;外部 AI 交叉验证,单包假设被证据修正)
- 2026-08-30(夜,APM Stage 1 收尾):17 条告警全定性(op-* 纯静默=合规缺陷 5/5;HD 0/5);**静默掉链重新定性=ATT 层冻结+host 重连**;GUI pcap 双录补齐;dryrun-test 排除;take_logs 删除;仓库入库基线 att-fuzz-fw
- 2026-08-30(文档重构):思路.md 并入 fuzz-plan.md 改为《设计.md》并删除;
  状态类内容全部收拢到本文件;使用指南 §7 改为指向本文件
- 2026-08-30:PDU 去重(663→572)/台账 signature 列;串口锁/GUI 持久化/历史页改造/
  gatt_map 排版;vivo 两次全量 663 条,17 告警,静默掉链发现
- 2026-08-29 前:阶段一完成 + 固件补丁(细节见《设计.md》§六与《使用指南.md》)
