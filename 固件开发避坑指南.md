# 固件开发避坑指南（Sniffle / CC2652R1F）

> 基于 2026-08-27 一次数小时的排查记录。症状以"串口静默"为主线，覆盖构建、烧录、调试全链路。
> 适用于：CC26X2R1_LAUNCHXL / CC1352R1_LAUNCHXL 及同 SDK 的其他平台。

---

## 0. 环境速查（本机）

| 组件 | 路径 |
|---|---|
| TI SimpleLink SDK 8.30.01.01 | `~/ti/simplelink_cc13xx_cc26xx_sdk_8_30_01_01`（makefile 默认路径） |
| SysConfig 1.28.1 | `~/ti/sysconfig-1.28.1/sysconfig_cli.sh`（SDK 要求 ≥1.21.1，1.28 实测兼容） |
| GCC 镜像树 | `GCC_ARMCOMPILER=/home/myx/ti/gcc-arm`（bin/ 软链系统 arm-none-eabi-gcc 13.2；arm-none-eabi/include/ 是 /usr/include/newlib 的**真实拷贝**，不能是软链） |
| UniFlash 9.6.0 / DSLite | `~/ti/uniflash_9.6.0/deskdb/content/TICloudAgent/linux/ccs_base/DebugServer/bin/DSLite` |
| xdsdfu / xds110reset | `~/ti/uniflash_9.6.0/deskdb/content/TICloudAgent/linux/ccs_base/common/uscif/xds110/` |
| 板子 | **CC2652R1F** LaunchPad（USB 枚举名写 CC1352R1，不可信，见 §2） |

完整编译命令模板：

```bash
cd fw && rm -f ti_drivers_config.* ti_sysbios_config.* ti_utils_build_linker.cmd.* *.obj *.out *.map *.hex
make -j4 PLATFORM=CC2652R1F \
  GCC_ARMCOMPILER=/home/myx/ti/gcc-arm \
  SYSCONFIG_TOOL=/home/myx/ti/sysconfig-1.28.1/sysconfig_cli.sh
```

---

## 1. 最大的坑：切换 PLATFORM 必须手动删生成文件

**症状**：换平台编译后，固件烧进去串口完全静默，但 JTAG/DSLite 调试器连接正常、读 flash/内存都正常。

**根因**：`make clean` 依赖 make **解析期**算出的 `SYSCFG_GEN_FILES`（`$(shell sysconfig --listGeneratedFiles ...)`）。换平台时若 clean 与实际构建不在同一 PLATFORM 上下文中，生成文件（`ti_drivers_config.c/h`、`ti_sysbios_config.c` 等）**不会被删除**，下次构建直接复用旧的——旧文件是上一个平台的板级配置，引脚全错。

**强制做法**（写进自己的构建脚本，别只信 make clean）：

```bash
rm -f ti_drivers_config.* ti_sysbios_config.* ti_utils_build_linker.cmd.* *.obj *.out *.map *.hex
```

**自检**：构建后打开 `ti_drivers_config.c`，看 GPIO 表里 `CONFIG_GPIO_UART2_0_RX/TX` 落在哪个 DIO 索引（对应 §2 的引脚表），以及文件时间戳是不是本次构建时间。

---

## 2. 平台/引脚知识

| 板子 | XDS110UART RX | XDS110UART TX | 其他特征 |
|---|---|---|---|
| **CC26X2R1_LAUNCHXL**（本板） | **DIO2** | **DIO3** | LED0=DIO6, LED1=DIO7 |
| CC1352R1_LAUNCHXL | DIO12 | DIO13 | 同家族芯片但引脚不同 |

- **两个平台的 hex 不可互换**：引脚错位 → 串口永静默（JTAG 不受影响，极易误判为固件坏/桥坏）。
- 板级定义在 SDK：`source/ti/boards/.meta/<BOARD>.syscfg.json` 的 `XDS110UART.connections`。
- **USB 枚举名不可信**：XDS110 的 iProduct 是板级标识，可能与实际芯片不符。以用户对硬件的确认 + 生成配置自检为准。
- 确认正确平台的最快办法：编译后查 `ti_drivers_config.c` 的 GPIO 表（§1 自检）。

---

## 3. 烧录与复位

**推荐流程（刷完确定在运行）**：

```bash
DSLite load -c fw/ccxml/CC2652R1F.ccxml -f <hex>            # load = 编程
~/ti/.../xds110/xds110reset -a toggle -d 100                 # 硬件复位，不碰调试器
# 然后串口探测
```

- **DSLite `flash ... -r 0` 的复位不保证解除 CPU halt**；`load` 文档说"留运行"，实测也不一定。**`xds110reset` 硬件复位最可靠**。
- **DSLite `memory` 读内存会把 CPU 挂起**（S_HALT=1）——"RAM 读两次无变化 = CPU 死了"是**假象**，是你自己读挂的。判断 CPU 是否在跑要用别的手段（§4）。
- 烧录权限：udev 规则已装（VID 0451 + 1cbe 全宽），无需 sudo。
- XDS110 升级固件（xdsdfu）必须先 `-m` 切 DFU 模式再 `-f`；DFU 模式 USB VID 是 **1cbe**（Luminary），不是 0451，udev 要覆盖两个。

---

## 4. 串口静默的排查决策树（按顺序）

```
① 引脚对不对？           查生成的 ti_drivers_config.c GPIO 表（§1）——本板 RX=DIO2/TX=DIO3
   ├─ 不对 → 重构建（rm 生成文件 + 确认 PLATFORM）
② CPU 在跑吗？           DSLite memory 读 0xE000EDF0 (DHCSR)
   ├─ S_HALT=1 → 是调试器挂的，xds110reset 复位后再测
   └─ 注意：读 DHCSR 本身也会挂起 CPU，所以顺序上先做完①和③
③ 链路通吗？             编译 SDK 自带 uart2echo 示例（tirtos7 版，CC26X2R1_LAUNCHXL），
                        烧进去发字节看回显——回显成功 = 桥和引脚都好的
   └─ 这也验证工具链本身没问题（SysConfig 1.28 + GCC 13 实测能出可用固件）
```

uart2echo 示例路径：
`~/ti/simplelink_cc13xx_cc26xx_sdk_8_30_01_01/examples/rtos/CC26X2R1_LAUNCHXL/drivers/uart2echo/tirtos7/gcc`
（`make GCC_ARMCOMPILER=... SYSCONFIG_TOOL=...`，生成 .out 后 `arm-none-eabi-objcopy -I elf32-little -O ihex uart2echo.out uart2echo.hex`）

---

## 5. 其他已知坑

- **XDS110 DFU 更新在 Linux 上 `DFU_ERR_UNKNOWN (-4)`**（权限修好也复现），在 Windows 上成功。遇到就上 Windows 的 UniFlash 点 Update，别在 Linux 上死磕。
- XDS110 升级过程中**两个 PID 的 udev 规则都要有**，否则升级切到编程模式瞬间断权限 → 板子会"消失"（lsusb 空），拔插即恢复，不会砖（ROM bootloader 兜底）。
- **pyOCD 连不上 XDS110 的 CMSIS-DAP**（cJTAG/SWD "No ACK"），别在 pyocd 上浪费时间；调试一律走 DSLite。
- 版本探测：`cd python_cli && python3 -c "from sniffle.sniffle_hw import SniffleHW; ... probe_fw_version()"`——注意必须 cd 到 python_cli，否则 ModuleNotFoundError。
- 探测超时后会有残留 python 进程占住串口：`fuser /dev/ttyACM0` 找到后 kill。
- 官方 release hex：`https://github.com/nccgroup/Sniffle/releases/download/v1.11.0/sniffle_cc2652r1.hex`（CC2652R1 平台，可用于对照）。

---

## 6. 本机已知正常配置（截至 2026-08-27）

- 补丁固件 1.12.0（COMMAND_TRANSMIT_AT 0x28 + TX 队列满上报）已烧录验证，8 秒抓 1484 条广播。
- 官方 1.11.0 与本工具链基线均能正常应答。
- 见《设计.md》的架构与平台事实、《进度.md》的当前状态。
